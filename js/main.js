"use strict";

// Add small, page-specific interactions here.
// Keep JavaScript modular: group related behavior in named functions.
document.addEventListener("DOMContentLoaded", () => {
  // Example: initializeNavigation();
});
